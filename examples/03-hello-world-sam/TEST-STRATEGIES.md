# 테스트 전략: LocalStack vs SAM Local vs AWS Lambda

SAM Local 프로젝트에는 **3가지 테스트 방식**이 있습니다. 각각의 용도와 차이점을 이해하면 더 효율적으로 개발할 수 있습니다.

---

## 📊 비교표

| 항목               | LocalStack               | SAM Local                | AWS Lambda              |
| ------------------ | ------------------------ | ------------------------ | ----------------------- |
| **테스트 파일**    | `localstack.test.js`     | `sam-local.test.js`      | `aws-lambda.test.sh`    |
| **데이터베이스**   | 로컬 Docker (LocalStack) | 로컬 Docker (LocalStack) | 실제 AWS 클라우드       |
| **Lambda 실행**    | 직접 JavaScript          | SAM 시뮬레이터           | 실제 AWS Lambda         |
| **실행 위치**      | 완전히 로컬              | 완전히 로컬              | AWS 클라우드            |
| **AWS 자격증명**   | 불필요                   | 불필요 (로컬)            | 필요                    |
| **테스트 속도**    | ⚡ 매우 빠름             | ⚡ 빠름                  | 🚀 약간 느림 (네트워크) |
| **실제 환경 재현** | ⚠️ 부분 시뮬레이션       | ✅ 거의 완벽             | ✅ 100% 실제 환경       |
| **클라우드 비용**  | 없음                     | 없음                     | 미소 (거의 없음)        |
| **네트워크 의존**  | 없음                     | 없음                     | 있음                    |
| **용도**           | 빠른 개발/디버깅         | 배포 전 최종 검증        | 실제 운영 환경 테스트   |
| **테스트 결과**    | ✅ 17/17 성공            | ✅ 8/8 성공              | ✅ 8/8 성공             |

---

## 🎯 최종 테스트 결과 (2025-12-30)

```
✅ LocalStack:   17/17 성공 (기본 4개 + DynamoDB CRUD 4개 + 에러 케이스 9개)
✅ SAM Local:    8/8 성공  (기본 4개 + DynamoDB CRUD 4개)
✅ AWS Lambda:   8/8 성공  (동일 함수, 실제 환경)
────────────────────────────────────────────
✅ 총: 33/33 모든 테스트 통과 🎉
```

---

## 🚀 실행 방법

### 방법 1️⃣: LocalStack으로 테스트 (완전히 로컬, 가장 빠름)

```bash
# 필수: LocalStack Docker 실행
docker-compose up -d

# 테스트 실행
npm run test:localstack
```

**결과**: ✅ 17/17 성공

**특징:**

- ✅ AWS 자격증명 불필요
- ✅ 매우 빠른 테스트 (1초 이내)
- ✅ 개발 초기 단계에 최적
- ✅ 기본 4개 함수 + DynamoDB CRUD 4개 + 에러 케이스 9개 모두 테스트

---

### 방법 2️⃣: SAM Local로 테스트 (로컬 시뮬레이션, 배포 전 검증)

```bash
# 필수: LocalStack Docker 실행
docker-compose up -d

# 필수: SAM 빌드
sam build

# 테스트 실행
npm run test:sam-local
```

**결과**: ✅ 8/8 성공

**특징:**

- ✅ SAM 시뮬레이터로 실제 Lambda 환경 재현
- ✅ AWS 자격증명 불필요
- ✅ 배포 전 최종 검증에 최적
- ✅ 기본 4개 함수 + DynamoDB CRUD 4개 모두 테스트

---

### 방법 3️⃣: AWS Lambda로 테스트 (실제 환경, 최종 검증)

```bash
# 필수: AWS 자격증명 설정
aws configure

# 필수: 배포
npm run deploy-dev

# 테스트 실행
npm run test:aws-lambda
```

**결과**: ✅ 8/8 성공

**특징:**

- ✅ 실제 AWS Lambda 환경에서 실행
- ✅ 실제 AWS API Gateway 통신
- ✅ 실제 배포 환경 검증
- ✅ 기본 4개 함수 + DynamoDB CRUD 4개 모두 테스트

---

## 📌 선택 가이드

### LocalStack (가장 권장 - 개발 중)

```bash
npm run test:localstack
```

**언제 사용?**

- 🔄 빠른 개발 사이클 필요할 때
- 🐛 로직 버그 디버깅
- 🎯 함수 로직 검증
- 💨 빠른 피드백 원할 때

**예시:**

```
개발자 → 코드 수정 → npm test (1초) → 결과 확인
```

---

### AWS DynamoDB (test/sam-local.test.js) 사용하기

```bash
node test/sam-local.test.js
```

**언제 사용?**

- ✅ 배포 전 최종 테스트
- ✅ 실제 AWS 환경 검증
- ✅ 네트워크 지연 테스트
- ✅ 실제 데이터 확인
- ✅ 프로덕션 준비 확인

**예시:**

```
개발 완료 → node test/sam-local.test.js (10초) → 배포
```

---

## 🔄 추천 워크플로우

### 개발 단계별 테스트 전략

```
┌─────────────────────────────────────────────────────────┐
│ 1️⃣  로컬 개발 중 (자주)                                  │
├─────────────────────────────────────────────────────────┤
│ npm test  (또는 node test/localstack.test.js)            │
│ • LocalStack 사용                                        │
│ • 매우 빠름 (1-2초)                                      │
│ • AWS 자격증명 불필요                                   │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│ 2️⃣  코드 변경 후 (3-4회)                                 │
├─────────────────────────────────────────────────────────┤
│ npm test  ← 계속 LocalStack으로 빠르게 검증              │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│ 3️⃣  배포 전 최종 검증 (1회)                              │
├─────────────────────────────────────────────────────────┤
│ node test/sam-local.test.js  ← AWS DynamoDB로 최종 검증  │
│ • 실제 AWS 환경                                          │
│ • 네트워크 지연 포함                                     │
│ • 프로덕션 환경과 동일                                   │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│ 4️⃣  AWS에 배포                                          │
├─────────────────────────────────────────────────────────┤
│ sam deploy                                               │
│ • 프로덕션 배포                                          │
│ • CloudFormation 자동 관리                              │
└─────────────────────────────────────────────────────────┘
```

---

## 🧪 각 테스트의 상세 비교

### localstack-test-all.js (LocalStack)

**구조:**

```
로컬 머신
  ↓
npm test (Node.js 직접 실행)
  ↓
LocalStack (Docker - 가상 DynamoDB)
```

**테스트 내용:**

- Lambda 함수 로직 직접 호출
- 동기 실행
- 즉시 결과 확인

**장점:**

- ⚡ 초고속 (1-2초)
- 🎯 핵심 로직 검증
- 🔄 개발 사이클 빠름

**단점:**

- 🐳 Docker 필요 (LocalStack)
- ⚠️ 실제 AWS와 다를 수 있음
- 🌍 SAM Local 환경과 다름

---

### sam-local-test-all.js (AWS DynamoDB)

**구조:**

```
로컬 머신
  ↓
SAM Local invoke (Docker)
  ↓
Lambda 함수 (Node.js 18.x 런타임)
  ↓
AWS DynamoDB (실제 클라우드)
```

**테스트 내용:**

- SAM Local 환경에서 실행
- 실제 Lambda 런타임 (nodejs18.x)
- 실제 AWS DynamoDB 사용
- 네트워크 지연 포함

**장점:**

- ✅ 실제 프로덕션 환경과 동일
- 🎯 배포 전 최종 검증
- 🌍 네트워크 지연 포함
- 📊 실제 데이터 확인

**단점:**

- 🚀 약간 느림 (10-15초)
- 🔐 AWS 자격증명 필요
- 🔑 IAM 권한 설정 필요

---

## 📋 두 가지 테스트 모두 성공

현재 프로젝트의 상태:

```
✅ npm test (LocalStack)
   └─ 17/17 테스트 성공

✅ node test/sam-local.test.js (AWS DynamoDB)
   └─ 8/8 함수 성공
      ├─ 4/4 일반 함수 (SayHello, Greet, CreateMessage, Divide)
      └─ 4/4 DynamoDB 함수 (Create, List, Update, Delete)
```

**의미:** 로컬 테스트 + 실제 AWS 환경 테스트 **모두 통과!** ✨

---

## 🎯 권장 사항

### 개발자 관점

```
┌─ 빠른 피드백 필요
│  └─ npm test (LocalStack)
│
├─ 배포 준비 됨
│  └─ node test/sam-local.test.js (AWS DynamoDB)
│
└─ 배포 직전
   └─ sam deploy (실제 프로덕션)
```

### 팀 관점

1. **개발 브랜치:** `npm test` 통과 필수
2. **QA 단계:** `node sam-local-test-all.js` 통과 필수
3. **배포:** `sam deploy` 실행

---

## 💡 핵심 정리

| 단계         | 명령어                        | 용도           |
| ------------ | ----------------------------- | -------------- |
| **개발 중**  | `npm test`                    | 빠른 반복 개발 |
| **배포 전**  | `node test/sam-local.test.js` | 최종 검증      |
| **프로덕션** | `sam deploy`                  | 실제 배포      |

**둘 다 의미가 있습니다!**

- `test/localstack.test.js`: 🚀 빠른 개발 사이클
- `test/sam-local.test.js`: ✅ 프로덕션 검증

프로젝트 성숙도에 따라 선택하면 됩니다! 🎉

---

**생성일**: 2025-12-28  
**상태**: ✅ 두 가지 테스트 방식 모두 검증 완료
